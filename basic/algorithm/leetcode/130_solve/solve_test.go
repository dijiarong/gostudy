package solve

import (
	"fmt"
	"testing"
)

func TestMain(t *testing.T) {
	board := [][]byte{
		{'X', 'X', 'X', 'X'},
		{'X', 'O', 'O', 'X'},
		{'X', 'X', 'O', 'X'},
		{'X', 'O', 'X', 'X'},
	}
	solve1(board)
	fmt.Println(board)
}
